using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using Logic.Core;

namespace Logic
{
    public class UI_TerroristEnter_Right : UIComponent
    {
        #region UI

        //private List<UI_Terrorist_LineTemplate> lineList = new List<UI_Terrorist_LineTemplate>();
        #endregion        

        protected override void Loaded()
        {
            //for (int i = 0; i < 3; ++i)
            //{
            //    string str = string.Format("Button_Line{0}", i + 1);
            //    UI_Terrorist_LineTemplate template = new UI_Terrorist_LineTemplate(transform.Find(str).gameObject);
            //    lineList.Add(template);
            //}
        }
    }
}


